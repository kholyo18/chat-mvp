{
  "name": "chat-mvp-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "build": "echo \"No build step\"",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.9.0",
    "googleapis": "^131.0.0",
    "stripe": "^14.22.0"
  }
}
