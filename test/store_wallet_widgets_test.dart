import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Store buy increments balance (stub)', (tester) async {},
      skip: 'Requires Firebase + widget setup.');

  testWidgets('Wallet renders transaction item (stub)', (tester) async {},
      skip: 'Requires Firebase + widget setup.');
}
