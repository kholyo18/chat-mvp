import 'package:flutter/material.dart';

class AdminRoomPanelPage extends StatelessWidget {
  const AdminRoomPanelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Admin Room Panel')),
      body: const Center(child: Text('Admin Room Panel — placeholder')),
    );
  }
}
